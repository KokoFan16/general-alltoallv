# Build directory (all objects + binaries)
build/

# Loose build artifacts (in case anyone builds without the Makefile)
*.o
*.dSYM/

# Data and plot artifacts (don't track raw results / generated figures)
data/
plots/*.png
plots/*.txt
plots/.ipynb_checkpoints/

# OS junk
.DS_Store
